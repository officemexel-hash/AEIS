"""
openhands.tools shim — tool implementations (name-only stubs).

SYLION uses these only for their `.name` attribute when building tool lists.
The actual tool execution happens through the Conversation runner, not through
individual tool classes.
"""
