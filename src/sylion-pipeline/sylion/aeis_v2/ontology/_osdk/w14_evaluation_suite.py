"""Auto-generated W15 OSDK client for `w14_evaluation_suite`.

Source manifest: metadata.id=w14_evaluation_suite version=v1.0
Generated by sylion.aeis_v2.ontology.osdk_gen.

DO NOT EDIT -- regenerate via POST /api/v1/ontology/regenerate-osdk.
"""
from __future__ import annotations

from typing import Any, TypedDict

import httpx


class W14EvaluationSuiteRow(TypedDict, total=False):
    """Row shape for w14_evaluation_suite (dedicated columns + extension)."""
    id: str
    legacy_w14_id: str
    target_function: str
    target_module: str
    test_case_count: int
    metric_count: int
    baseline_run_id: str | None
    extension: dict[str, Any]
    created_at: str
    updated_at: str


class W14EvaluationSuiteClient:
    """HTTP client for `w14_evaluation_suite` ontology type."""

    def __init__(self, base_url: str = "http://127.0.0.1:8000",
                  api_key: str | None = None,
                  timeout: float = 10.0) -> None:
        self.base_url = base_url.rstrip("/")
        self.type_id = "w14_evaluation_suite"
        self.timeout = timeout
        self._headers: dict[str, str] = {"Content-Type": "application/json"}
        if api_key:
            self._headers["Authorization"] = f"Bearer {api_key}"

    def get(self, instance_id: str) -> W14EvaluationSuiteRow:
        """Fetch one w14_evaluation_suite instance by id."""
        with httpx.Client(timeout=self.timeout) as c:
            r = c.get(
                f"{self.base_url}/api/v1/ontology/w14_evaluation_suite/{instance_id}",
                headers=self._headers,
            )
            r.raise_for_status()
            return r.json()

    def create(self, **fields: Any) -> W14EvaluationSuiteRow:
        """Create a new w14_evaluation_suite instance.

        Phase 0: returns NotImplementedError until W15 G2 instance
        endpoints land. Stub kept so consumers can import.
        """
        raise NotImplementedError(
            "create({fields}) requires W15 G2 instance endpoints"
        )

    def list(self, *, limit: int = 50, offset: int = 0) -> list[W14EvaluationSuiteRow]:
        """List w14_evaluation_suite instances."""
        raise NotImplementedError(
            "list() requires W15 G2 instance endpoints"
        )

    def update(self, instance_id: str, **fields: Any) -> W14EvaluationSuiteRow:
        """Update fields on a w14_evaluation_suite instance."""
        raise NotImplementedError("update() requires W15 G2")

    def delete(self, instance_id: str) -> bool:
        """Soft-delete a w14_evaluation_suite instance."""
        raise NotImplementedError("delete() requires W15 G2")

    def refresh_baseline_evaluation(self, instance_id: str, **payload: Any) -> dict[str, Any]:
        """Action `refresh_baseline_evaluation` (D-level=D2, hg_required=False).

        PL: Odświeża baseline dla pakietu ewaluacyjnego po nowym przebiegu.
        EN: Refreshes the baseline for the evaluation suite after a new run.
        """
        with httpx.Client(timeout=self.timeout) as c:
            r = c.post(
                f"{self.base_url}/api/v1/ontology/w14_evaluation_suite/{instance_id}/actions/refresh_baseline_evaluation",
                headers=self._headers, json=payload,
            )
            r.raise_for_status()
            return r.json()

    def freeze_evaluation_suite(self, instance_id: str, **payload: Any) -> dict[str, Any]:
        """Action `freeze_evaluation_suite` (D-level=D2, hg_required=False).

        PL: Zamyka konfigurację evals do porównań regresyjnych.
        EN: Freezes the evals configuration for regression comparisons.
        """
        with httpx.Client(timeout=self.timeout) as c:
            r = c.post(
                f"{self.base_url}/api/v1/ontology/w14_evaluation_suite/{instance_id}/actions/freeze_evaluation_suite",
                headers=self._headers, json=payload,
            )
            r.raise_for_status()
            return r.json()

