"""Auto-generated W15 OSDK client for `w14_repair_attempt`.

Source manifest: metadata.id=w14_repair_attempt version=v1.0
Generated by sylion.aeis_v2.ontology.osdk_gen.

DO NOT EDIT -- regenerate via POST /api/v1/ontology/regenerate-osdk.
"""
from __future__ import annotations

from typing import Any, TypedDict

import httpx


class W14RepairAttemptRow(TypedDict, total=False):
    """Row shape for w14_repair_attempt (dedicated columns + extension)."""
    id: str
    legacy_w14_id: str
    attempt_number: int
    r_phase: str
    result: str
    files_touched_count: int
    diff_lines: int
    time_in_phase_s: float
    cost_usd: float
    finding_id: str | None
    patch_proposal_id: str | None
    extension: dict[str, Any]
    created_at: str
    updated_at: str


class W14RepairAttemptClient:
    """HTTP client for `w14_repair_attempt` ontology type."""

    def __init__(self, base_url: str = "http://127.0.0.1:8000",
                  api_key: str | None = None,
                  timeout: float = 10.0) -> None:
        self.base_url = base_url.rstrip("/")
        self.type_id = "w14_repair_attempt"
        self.timeout = timeout
        self._headers: dict[str, str] = {"Content-Type": "application/json"}
        if api_key:
            self._headers["Authorization"] = f"Bearer {api_key}"

    def get(self, instance_id: str) -> W14RepairAttemptRow:
        """Fetch one w14_repair_attempt instance by id."""
        with httpx.Client(timeout=self.timeout) as c:
            r = c.get(
                f"{self.base_url}/api/v1/ontology/w14_repair_attempt/{instance_id}",
                headers=self._headers,
            )
            r.raise_for_status()
            return r.json()

    def create(self, **fields: Any) -> W14RepairAttemptRow:
        """Create a new w14_repair_attempt instance.

        Phase 0: returns NotImplementedError until W15 G2 instance
        endpoints land. Stub kept so consumers can import.
        """
        raise NotImplementedError(
            "create({fields}) requires W15 G2 instance endpoints"
        )

    def list(self, *, limit: int = 50, offset: int = 0) -> list[W14RepairAttemptRow]:
        """List w14_repair_attempt instances."""
        raise NotImplementedError(
            "list() requires W15 G2 instance endpoints"
        )

    def update(self, instance_id: str, **fields: Any) -> W14RepairAttemptRow:
        """Update fields on a w14_repair_attempt instance."""
        raise NotImplementedError("update() requires W15 G2")

    def delete(self, instance_id: str) -> bool:
        """Soft-delete a w14_repair_attempt instance."""
        raise NotImplementedError("delete() requires W15 G2")

    def mark_repair_attempt_verified(self, instance_id: str, **payload: Any) -> dict[str, Any]:
        """Action `mark_repair_attempt_verified` (D-level=D2, hg_required=False).

        PL: Oznacza próbę naprawy jako zweryfikowaną po retescie.
        EN: Marks the repair attempt as verified after retest.
        """
        with httpx.Client(timeout=self.timeout) as c:
            r = c.post(
                f"{self.base_url}/api/v1/ontology/w14_repair_attempt/{instance_id}/actions/mark_repair_attempt_verified",
                headers=self._headers, json=payload,
            )
            r.raise_for_status()
            return r.json()

    def escalate_repair_attempt(self, instance_id: str, **payload: Any) -> dict[str, Any]:
        """Action `escalate_repair_attempt` (D-level=D3, hg_required=True).

        PL: Eskaluje próbę naprawy po braku postępu lub ryzyku ubocznym.
        EN: Escalates the repair attempt after lack of progress or side-effect risk.
        """
        with httpx.Client(timeout=self.timeout) as c:
            r = c.post(
                f"{self.base_url}/api/v1/ontology/w14_repair_attempt/{instance_id}/actions/escalate_repair_attempt",
                headers=self._headers, json=payload,
            )
            r.raise_for_status()
            return r.json()

