"""W14 E11 demo projects — full per-project implementations.

Each demo lives under its own subpackage:
  - mobile_field_inspector  — D4 mobile app for field inspections
  - public_project_showcase  — D3 web portal (next iter)
  - factory_automation_panel — D5 industrial IoT (next iter)
  - operator_crm             — D4 CRM (next iter)
  - funding_pipeline_tracker — D4 grants (next iter)
  - skills_marketplace       — D5 marketplace (next iter)

Each implementation exercises real W14 lifecycle through the testing
package (Charter -> Findings -> Repair -> Release -> Memory).
"""
