"""
SYLION gRPC -- Generated Stubs

Auto-generated gRPC Python stubs from proto definitions.
Regenerate with: python -m grpc_tools.protoc -I proto --python_out=sylion/grpc_stubs --grpc_python_out=sylion/grpc_stubs proto/*.proto
"""
import os
import sys

# Proto-generated files use bare imports (e.g., `import sylion_common_pb2`).
# Add this directory to sys.path so those imports resolve.
_stubs_dir = os.path.dirname(__file__)
if _stubs_dir not in sys.path:
    sys.path.insert(0, _stubs_dir)
