# =============================================================================
# SYLION AEIS — Kustomize manifest
# =============================================================================
# Apply with:  kubectl apply -k k8s/
# Or via:       scripts/deploy_k8s.sh
# =============================================================================

apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: sylion

commonLabels:
  app.kubernetes.io/part-of: sylion-aeis
  app.kubernetes.io/managed-by: kustomize

resources:
  - namespace.yaml
  - postgres.yaml
  - nats.yaml
  - api.yaml
  - ingress.yaml
