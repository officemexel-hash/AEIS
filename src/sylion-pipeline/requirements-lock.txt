# SYLION Pipeline — Pinned dependencies
# Generated: 2026-04-11 (updated: 2026-04-19 SEQ-001/SEQ-005 security patches)
# Python: >=3.11,<3.14
# Update with: pip-compile requirements.in -o requirements-lock.txt
# IMPORTANT (SEQ-001): ALWAYS run with venv python, NEVER with system python.
#   Enforce: source .venv/bin/activate && pip install -r requirements-lock.txt
#   CI check: python -c "import aiohttp; assert aiohttp.__version__ == '3.13.5'"
#   Pre-flight: pip-audit -r requirements-lock.txt --fail-on-vuln
# Security patch: 2026-04-11 — aiohttp CVE batch fix (v5.9.1-sec-p1)
#   aiohttp 3.13.3 → 3.13.5  (10 CVE HIGH — see docs/pip_audit_report.md)
#     CVE-2026-22815 HIGH  — uncapped memory usage in header/trailer handling
#     CVE-2026-34513 HIGH  — request smuggling via content_type injection
#     CVE-2026-34514 HIGH  — NTLMv2 remote path info disclosure (Windows)
#     CVE-2026-34515 HIGH  — Authorization header leaked on cross-origin redirect
#     CVE-2026-34516 HIGH  — DoS via excessive multipart headers memory usage
#     CVE-2026-34519 HIGH  — HTTP Response Splitting (XSS/cache-poisoning)
#     CVE-2026-34520 HIGH  — DNS cache DoS (unbounded memory usage)
#     CVE-2026-34525 HIGH  — Request smuggling via multiple Host headers
#     CVE-2025-69223 HIGH  — DoS via zip bomb / compressed request
#     CVE-2025-69227 HIGH  — DoS via infinite loop in POST body handling
#
# Security patch: 2026-04-19 — Cluster D CVE fix (v5.9.1)
#   starlette 0.46.2 → 0.49.1  (CVE-2025-62727 HIGH, CVE-2025-54121 Medium)
#   fastapi 0.115.12 → 0.136.0  (required for starlette >=0.49 compat)
#   python-multipart 0.0.20 → 0.0.27  (CVE-2026-24486 HIGH, CVE-2026-40347 Medium, CVE-2026-42561)
#   pypdf 5.4.0 → 6.10.2  (22 CVEs DoS)
#   litellm 1.67.4.post1 → 1.83.10  (CVE-2026-35030 CRITICAL, CVE-2026-35029 HIGH, GHSA-69x8-hrgq-fjj8 HIGH, CVE-2026-42203/42208/42271/40217)
#   pytest 8.3.4 → 9.0.3  (CVE-2025-71176 Medium local priv-esc)
#   fastuuid 0.14.0 — new transitive dep pulled in by litellm>=1.83.0
#
# TRANSITIVE aiohttp CHAIN:
#   litellm==1.83.10 → aiohttp>=3.10  (direct require)
#   openai==2.24.0  → aiohttp (optional extra 'aiohttp')
#   NOTE: aiohttp was NOT previously pinned in this lockfile — this patch adds it.

# === Core framework ===
fastapi==0.136.0
uvicorn==0.34.2
starlette==0.49.1
pydantic==2.11.1
pydantic-core==2.33.0

# === Password hashing (MANDATORY — do NOT remove) ===
argon2-cffi==23.1.0
argon2-cffi-bindings==21.2.0

# === Async file I/O (non-blocking upload streaming — Council v5.8) ===
aiofiles==24.1.0

# === Form/file upload (required by FastAPI UploadFile) ===
python-multipart==0.0.27

# === Document text extraction (Baseline Center: PDF/DOCX upload) ===
pypdf==6.10.2
python-docx==1.1.2

# === Environment & config ===
python-dotenv==1.2.2
pyyaml==6.0.2

# === HTTP client ===
httpx==0.28.1
anyio==4.9.0

# === LLM providers ===
# v5.9.1 Security fix: 1.83.10 resolves known 1.83.0/1.83.7 CVEs.
litellm==1.83.10
# fastuuid is a direct dep of litellm>=1.83.0 (UUID generation performance)
fastuuid==0.14.0

# === Numeric / ML (SemanticDedup, BenchmarkHarness) ===
numpy==1.26.4

# === CLI / UI ===
rich==14.0.0

# === File watching ===
watchdog==6.0.0

# === Utilities ===
typing-extensions==4.13.2
sniffio==1.3.1
certifi==2025.1.31
idna==3.10

# === JWT authentication (P8/Wave9: jwt_auth.py) ===
# CP-1: pinned exact version for lockfile integrity
# SECURITY PATCH 2026-05-13: PyJWT 2.10.1 -> 2.12.0
# CVE-2026-32597 (GHSA-752w-5fwx-jx9f) — missing 'crit' header validation
# Fix version: PyJWT >= 2.12.0. Runtime already 2.12.1; lockfile updated for
# clean-install environments. See reports/security_audit_report.md SEQ-005.
PyJWT==2.12.0

# === Prometheus metrics (P8/Wave9: metrics.py + /api/metrics/prom endpoint) ===
# CP-1: pinned exact version for lockfile integrity
prometheus-client==0.20.0

# === Testing ===
pytest==9.0.3

# =============================================================================
# SECURITY PATCH 2026-04-11: aiohttp transitive CVE fix
# Previously UNPINNED — aiohttp was a transitive dep of litellm but not
# explicitly listed in this lockfile, leaving the resolved version to pip's
# resolver (currently resolving to 3.13.3 which carries 10 HIGH CVEs).
#
# Target: aiohttp==3.13.5 (latest stable, all CVEs fixed, Python >=3.9 compat)
# Verified: Python 3.12 wheels available on PyPI (cp312-cp312 wheels present)
#
# aiohttp and its transitive deps (all pinned below):
# =============================================================================
aiohttp==3.13.5
aiohappyeyeballs==2.6.1
aiosignal==1.4.0
attrs==26.1.0
frozenlist==1.8.0
multidict==6.7.1
propcache==0.4.1
yarl==1.23.0

# =============================================================================
# T-05 PATCH v6.0.0: missing security and ML dependencies
# bcrypt: password hashing fallback (argon2-cffi preferred, bcrypt fallback)
# sentence-transformers + scikit-learn: used by semantic_dedup.py
# =============================================================================
bcrypt==4.1.3
sentence-transformers==3.0.1
scikit-learn==1.5.2
